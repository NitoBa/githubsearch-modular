import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:gobarber/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // ReposController repos;
  //
  setUp(() {
    //     repos = HomeModule.to.get<ReposController>();
  });

  group('ReposController Test', () {
    //   test("First Test", () {
    //     expect(repos, isInstanceOf<ReposController>());
    //   });

    //   test("Set Value", () {
    //     expect(repos.value, equals(0));
    //     repos.increment();
    //     expect(repos.value, equals(1));
    //   });
  });
}
